[ ] Revisit and exhaust all methods for interrupting the boot process (ESC, ATGU, specific timing, etc.)
    [ ] Try automating boot interruption with an `expect` script
[ ] If boot interruption fails, investigate methods to force the web GUI into a recovery mode or accessible state
[ ] If web GUI access is achieved, attempt to enable SSH/FTP or use the `_encrypt_` oracle via config backup/restore
[ ] If all else fails, research new vulnerabilities for the Zyxel NR7101
